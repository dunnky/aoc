import { read } from '.'
import { solve } from './00'

describe('Day 00', () => {
  describe('Part 1', () => {
    it('Solution', () => {
      expect(solve(read())).toBe('0')
    })
  })
  describe('Part 2', () => {
    it('Solution', () => {
      expect(solve(read())).toBe('0')
    })
  })
})
