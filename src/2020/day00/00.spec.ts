import { read } from '.'
import { solve } from './00'

describe('Day 00', () => {
  it('Part 1', () => {
    expect(solve(read())).toBe('0')
  })
  it('Part 2', () => {
    expect(solve(read())).toBe('0')
  })
})
